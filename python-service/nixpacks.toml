[phases.setup]
aptPkgs = ["libgl1", "libglib2.0-0", "libxcb1", "libxrender1", "libxext6", "libsm6"]

[phases.install]
cmds = ["pip install --upgrade pip", "pip install -r requirements.txt"]

[start]
cmd = "uvicorn main:app --host 0.0.0.0 --port ${PORT:-8000}"
